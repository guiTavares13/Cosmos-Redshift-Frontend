import React from "react";

export default function ImageLinearRegressionComponent() {
    return (
        <div style={{ display: 'flex', justifyContent: 'center', paddingTop: 30 }}>
            <img src={''} alt="Exemplo de correlação" />
        </div>
    );
}